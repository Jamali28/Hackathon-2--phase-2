spec:
  containers:
  - name: backend
    image: python:3.11-slim
    setup: |
      pip install -r requirements.txt
    command: |
      uvicorn main:app --host 0.0.0.0 --port 7860
    env:
    - name: DATABASE_URL
      value: ""
    ports:
    - containerPort: 7860
      name: http